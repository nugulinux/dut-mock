<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <link rel="stylesheet" href="/stylesheets/bootstrap.min.css" />
  <title><%= title %></title>
</head>

<body>
  <nav class="navbar navbar-expand-md navbar-dark bg-primary fixed-top shadow">
    <span class="navbar-brand"><%= title %></span>
    <div class="navbar-collapse">
      <ul class="navbar-nav">
        <li class="nav-item <% if (active_menu == "/") { %>active<% } %>">
          <a class="nav-link" href="/">Home</a>
        </li>
        <li class="nav-item <% if (active_menu == "/registry") { %>active<% } %>">
          <a class="nav-link" href="/registry">Registry
            <% if (registry.child) { %>
            <span class="badge badge-secondary ml-1">on</span>
            <% } else {%>
            <span class="badge badge-secondary ml-1">off</span>
            <% } %>
          </a>
        </li>
        <li class="nav-item <% if (active_menu == "/server") { %>active<% } %>">
          <a class="nav-link" href="/server">Servers<span
              class="badge badge-secondary ml-1"><%=server_list.length%></span></a>
        </li>
      </ul>
    </div>
  </nav>

  <div class="container" role="main">